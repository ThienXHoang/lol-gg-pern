import NavBar from "../components/NavBar";
import SearchBar from "../components/SearchBar";

const Home = () => {
  return (
    <div>
      <NavBar />
      <SearchBar />
    </div>
  );
};

export default Home;
